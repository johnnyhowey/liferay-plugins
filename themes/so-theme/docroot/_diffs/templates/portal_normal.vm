<!DOCTYPE html>

#parse ($init)

<html class="#language("lang.dir")" dir="#language("lang.dir")" lang="$w3c_language_id">

<head>
	<title>$the_title - $company_name</title>

	$theme.include($top_head_include)
</head>


<body class="$css_class">

$theme.include($body_top_include)

#if ($is_signed_in)
	#dockbar()
#end

<div id="wrapper">
	<a href="#main-content" id="skip-to-content">#language("skip-to-content")</a>

	<header id="banner" role="banner">
		<hgroup id="heading">
			<div class="company-title">
				<a class="$logo" href="$company_url" title="#language("go-to") $company_name">
					<span>$company_name</span>
				</a>
			</div>

			#if (!$is_profile_page)
				<div class="community-title">
					<a href="$community_default_url" title="#language("go-to") $community_name">
						<span>$community_name</span>
					</a>
				</div>
			#end

			<h3 class="page-title">
				<span>$the_title</span>
			</h3>

			#if ($is_signed_in)
				<div id="page-search">
					$theme.search()
				</div>
			#end
		</hgroup>

		#if (!$is_signed_in)
			<a href="$sign_in_url" id="sign-in" rel="nofollow">$sign_in_text</a>
		#end
	</header>
	<div id="content">
		<div id="so-sidebar">
			#if ($is_profile_page)
				<img src="$currentUser.getPortraitURL($themeDisplay)" alt="" class="profile-image">
			#end
			#if ($has_navigation || $userPage)
				#parse ("$full_templates_path/navigation.vm")
			#end

			#if ($is_profile_page || $userPage)
				#parse ("$full_templates_path/sidebar_portlets.vm")
			#end
		</div>

		<div id="so-context">
			#if ($is_profile_page)
				<div class="profile-header">
					<h1 class="user-name">$currentUserName</h1>
					
					#if ($currentUser.getJobTitle() != "")
						<span class="user-job-title">$currentUser.getJobTitle()</span>
					#end

					#if ($currentUser.getEmailAddress != "")
						<span class="user-email-address"><a href="mailto:$currentUser.getEmailAddress()">$currentUser.getEmailAddress()</a></span>
					#end
				</div>
			#end

			#if ($selectable)
				$theme.include($content_include)
			#else
				$portletDisplay.recycle()

				$portletDisplay.setTitle($the_title)

				$theme.wrapPortlet("portlet.vm", $content_include)
			#end
		</div>
	</div>

	<footer id="footer" role="contentinfo">
		<p class="powered-by">
			#language("powered-by") <a href="http://www.liferay.com" rel="external">Liferay</a>
		</p>
	</footer>
</div>

$theme.include($body_bottom_include)

</body>

$theme.include($bottom_include)

</html>